# 포디엄 버그 기록

고친 버그는 **빠짐없이 여기 적는다** (2026-08-25 사용자 지시). CLAUDE.md 의 '함정' 절은
*앞으로 이렇게 하라*는 규칙을 담고, 이 파일은 *무엇이 어떻게 깨졌었나*는 이력을 담는다.
같은 증상이 몇 달 뒤 다시 올라올 때 "그때 뭐였더라"를 되풀이하지 않기 위한 것이다.

## 적는 방법

새 항목은 **맨 위에** 쌓는다. 한 건에 다섯 줄:

```
### YYYY-MM-DD  한 줄 제목
- **증상** 사용자·화면에서 무엇이 잘못 보였나 (실제 값을 그대로 인용)
- **원인** 왜 그랬나. 코드 한 줄이면 그 줄을, 구조 문제면 구조를
- **수정** 무엇을 어떻게 바꿨나 (함수·파일 이름)
- **재발 방지** 같은 실수가 다시 나면 어디서 잡히나. 없으면 '없음'이라고 적을 것
- **커밋** 해시
```

**발견 경위도 한 줄 적는다** — 사용자 제보인지, 로그인지, 대조 중 우연인지. 어느 경로가
실제로 버그를 잡아내는지가 쌓이면 그게 곧 검수 전략이다. 지금까지는 **사용자가 카드
스크린샷을 던진 것**이 압도적으로 많다.

---

### 2026-08-26  광신대 '공식 공고 페이지' 버튼이 게시판 목록으로 떨어짐
- **증상** 카드의 '공식 공고 페이지 바로가기' 를 누르면 광신대 공지사항 **목록**이 열린다.
  방문자에게는 링크가 깨진 것으로 보인다.
- **원인** 셋이 겹쳤다. (1) 하이브레인이 준 원문 링크 자체가 게시판 목록이다(08-23 기록).
  (2) 그 게시판의 상세 링크가 `javascript:fView('42748')` 꼴이라 08-23 에 '교정 불가'로
  단정하고 건너뛰었는데, 실은 목록 URL 의 List→View 치환 + 글 번호로 GET 주소를 조립할 수
  있었다. (3) 다만 광신대는 하이브레인이 제목을 고쳐 실어('강사 모집 (재공고)' vs '2026학년도
  2학기 교원(강사) 임용 추가공고 안내문') 낱말 대조가 후보를 확신하지 못하고, 후보도 둘이라
  자동 선택이 원리적으로 위험하다 — 그런데 화면은 목록 링크에 '공식 공고 페이지'라는 라벨을
  그대로 붙이고 있었다.
- **수정** ① `_find_detail_in_list` 가 `javascript:` 상세도 받는다 — 글 번호로 주소를
  조립하고, 조립 주소는 열어서 그 글 제목이 페이지에 있는지 대조 후 채택(틀리면 폐기).
  ② 그래도 목록에 머무는 공고(`originIsList`)는 버튼 라벨을 **'기관 게시판에서 공고 찾기 ↗'**
  로 — jobs.js·staticgen._apply 양쪽.
- **재발 방지** 조립 주소는 제목 대조를 통과해야만 쓰인다(남의 공고로 보낼 수 없다).
  라벨은 originIsList 가 남아 있는 한 자동으로 정직해진다.
- **발견 경위** 사용자 제보("광신대 링크 문제생긴듯. 게시판 메인화면으로 간다").
- **커밋** (이 커밋)

### 2026-08-26  기독정보넷이 403 봇 차단인데 '파서 깨짐'으로 뜸
- **증상** 🔴 `기독정보넷(교회 반주): 원본 0건 (평소 24건) — 목록 파서 깨짐 의심`.
  어제 18시까지 24건이 나오다 새벽 1시부터 0건.
- **원인** 사이트가 Cloudflare 봇 차단 뒤로 들어가 403 + 'Just a moment…' 를 준다.
  그 페이지도 본문은 멀쩡한 HTML 이라 목록 파서가 예외 없이 0건을 돌려줬다.
  08-22 의 경남교육청 503 과 **완전히 같은 구조** — 그때 5xx 만 올리게 하고 4xx 를 안 봤다.
- **수정** `common.get()` 이 403 도 예외로 올린다. 404 는 그대로 둔다(빈 목록을 404 로
  주는 게시판이 있어 호출부가 읽는다).
- **재발 방지** 이제 '403 차단'으로 뜨고 `classify_error` 가 비일시적으로 분류해 첫날부터
  알린다. **차단 자체는 우회하지 않는다** — 접근 방법은 사람이 정할 일이다.
- **발견 경위** 다른 건 고치고 헬스체크 재확인하다 새로 뜬 것.
- **커밋** 210d588

### 2026-08-26  '수집 0건'을 전부 분류기 고장으로 읽음 (KBS·광주시립·안동 🔴 3건)
- **증상** 🔴 `원본 10건인데 수집 0건 (평소 3건) — 분류·필터 깨짐 의심` 이 세 소스에서
  동시에. 실제로는 아무것도 안 깨져 있었다.
- **원인** 셋 다 지난 채용 공고가 계속 걸려 있는 아카이브형 게시판이다. enrich 가 18일간
  죽어 있는 동안(2896855) 마감을 못 읽어 '기한 미정'으로 남아 있다가, 마감을 다시 읽게
  되자 만료 공고로 걷혔다. 원본을 열어 보니 KBS 2026-08-02, 광주 2026-06-18, 안동
  2026-07-26 — 전부 지난 공고. baseline(평소 3~5건)이 그 18일 동안 쌓인 값이라
  45일 median 이 0을 따라가는 데 3주가 걸린다.
- **수정** 크롤이 원본을 거를 때 **사유별로 세어**(무관·비음악·오래됨·지난해·만료)
  소스 요약에 `drop` 으로 싣고, 헬스체크가 그걸 읽는다. 지난 공고가 원본의 절반 이상이면
  🔴 대신 ⚪ '원본 N건이 전부 지난 공고 — 정상'.
- **재발 방지** 같은 물음('수집 0건인데 고장인가')이 다시 와도 짐작이 아니라 사유를 본다.
  하이브레인에 `declared`(사이트가 밝힌 건수)를 넘긴 것과 같은 방식 — **판정에 쓸 사실이
  크롤 쪽에 있으면 넘겨라. baseline 은 사실이 없을 때의 차선이다.**
- **발견 경위** 사용자가 브리핑을 붙여 넣고 "가볍게 확인좀".
- **커밋** 210d588

### 2026-08-26  인원 칸에 표가 통째로 (L4#6 샛별중)
- **증상** 카드 '모집' 행에 `교과 구분 채용 기간 인원 비고 음악 기간제 교사 2026. 9. 14.
  ~ 2027. 2. 22.(5개월 9일(2학기)) 1명 담임교사 - 순회교사 겸임`. 맞는 값은 `1명`.
- **원인** 같은 표의 납작한 칸에 `채용인원 1명` 이 멀쩡히 있는데도, 첨부 hwp 의 표 한 판에
  콜론이 붙어 있다는 이유로 강한 모드가 그걸 먼저 받았다. 08-22 망포중 근무기간
  ('상기 채용 기간 참고')과 같은 뿌리 — **콜론이 붙었다고 다 값은 아니다.**
  QC 에 '헤더 낱말 연쇄' 규칙이 있었지만 `채용기간` 처럼 붙여 쓴 꼴만 알아 `채용 기간` 이
  샜고, 게다가 QC 는 화면 직전이라 걸러 봐야 빈칸이 된다.
- **수정** `common._HEAD_RUN_LEAD` — 칸 이름이 셋 이상 줄줄이 앞에 붙으면 값이 아니라
  표로 보고 미뤄 둔다(08-22 에 만든 weak 폴백에 태운다). 그러면 납작한 칸에서 `1명` 을
  다시 찾아온다. 띄어 쓴 칸 이름까지 받는다.
- **재발 방지** 추출 단계에서 잡으므로 값을 잃지 않는다 — **QC 는 마지막 그물이지 판정
  자리가 아니다.** 보관 원문 1,228건 A/B 에서 바뀌는 값은 이 한 건뿐임을 확인했다.
- **발견 경위** L4 상식 검증(#6, high). 규칙 검수 5층을 다 통과한 값이었다.
- **커밋** 210d588

### 2026-08-25  enrich 가 18일간 통째로 죽어 있었다 (조용한 죽음 3중)
- **증상** 크롤 로그에 `enrich 실패`가 회차마다 80~90건. 마감·조건이 안 채워지는 공고가
  계속 나오는데 이유를 알 수 없었다.
- **원인** `except Exception:` 이 예외를 삼키고 URL 만 남겨서 18일간 아무도 몰랐다.
  예외를 찍게 하자 셋이 겹쳐 있었다:
  1. `NameError: strip_navi` — main.py 에 import 누락. **2026-08-07(21ed849, GA4 계측
     확장)부터** `enrich_deadline` 이 첫 줄에서 죽었다. 마감 보강 경로가 한 번도 안 돌았고,
     '마감을 못 찾는다'고 본 것 상당수가 실은 찾으려는 시도조차 안 한 것이었다.
  2. `TypeError: _body_excerpt() got an unexpected keyword argument 'org'` — 1번에 가려
     보이지 않았다. 호출부만 고쳐지고 정의가 되돌아가 있었다(세션 두 개가 같은 파일을
     동시에 만진 결과로 보인다).
  3. `AttributeError: 'Session' object has no attribute 'find_all'` — `_trace_origin` 의
     첫 인자는 soup 인데 이름이 `s` 라 호출부가 세션을 넘겼다. 아트인포 원출처 역추적이
     통째로 죽어 있었다.
- **수정** import 복구 · `_body_excerpt(soup, title, org)` 시그니처 복구 ·
  `_trace_origin(soup, ...)` 로 인자 이름 변경 · **예외 종류와 메시지를 로그에 남기게 함**.
- **재발 방지** 로그가 `enrich 실패 [ExceptionType] 메시지 URL` 로 찍힌다. 같은 일이
  생기면 로그만 봐도 바로 보인다. **`except Exception:` 으로 예외를 삼키지 말 것** —
  매일 80건씩 실패해도 아무도 모른다.
- **곁가지** 고친 뒤 소스별 `kept` 가 88→43 으로 줄어 회귀인 줄 알았으나 정화였다.
  마감일을 얻은 지난 공고들이 '마감 지난 공고 제거' 단계에서 제때 걸러진 것이다.
  raw(수집 총량) 411→412 그대로, 최종 게시 27→28 유지로 확인했다.
- **발견 경위** 사용자 지시("엔리치 실패 파봐"). 그전까지 로그의 상수로 취급하고 있었다.
- **커밋** 2896855

### 2026-08-25  학교 안 동아리를 남의 기관으로 오인해 학교 딱지가 안 붙음
- **증상** `성남고등학교 윈드오케스트라 클라리넷 강사 채용` 이 `[성남고]` 없이 나갔다.
- **원인** `school_title` 의 '제목이 이미 다른 기관을 말하면 딱지를 붙이지 않는다' 가드가
  `성남고등학교 윈드오케스트라` 를 통째로 남의 단체로 읽었다. 그 가드 자체는 옳다 —
  연세대 게시판에 올라온 천안시립교향악단 공고를 `[연세대학교] …` 로 만들지 않으려는 것.
- **수정** 매치된 앞머리가 **그 공고의 org 자신**이면 가드를 건너뛴다.
- **재발 방지** 없음(규칙 보강뿐). 다른 '동아리를 기관으로 읽는' 사례가 나오면 같은 자리.
- **발견 경위** 사용자 카드 스크린샷.
- **커밋** 2896855

### 2026-08-25  제목 꼬리에 과목·인원이 중복
- **증상** `[목운중] 시간강사 채용 공고: 음악 1명` — 모집(1명)·담당업무(음악) 행에 이미
  실리는 값이 제목에도 붙었다.
- **원인** 규칙 없음(미구현).
- **수정** `_TAIL_SUBJ_COUNT` 로 제목 끝의 `: 과목 N명` 꼴 제거.
- **재발 방지** 없음.
- **발견 경위** 사용자 카드 스크린샷.
- **커밋** 2896855

### 2026-08-25  표에서 라벨과 값이 갈라져 급여를 절반만 실음
- **증상** 양주시립교향악단 지휘자 공고 페이가 `150,000원`. 원문은 `기본급 1,990,000원/월,
  연주수당 150,000원/회` 였다. **15만원만 보이면 오해를 부르니 빈칸보다 나쁘다.**
- **원인** hwp 표를 평탄화하면 헤더 행과 값 행이 한 줄로 붙는다 —
  `직위 신분 보수 근무시간 위촉기간 지휘자 비상임 기본급 1,990,000원/월, …`.
  라벨(`보수`)과 값 사이에 헤더 셋이 끼어 `라벨 : 값` 규칙이 짝을 못 맞추고, `보수` 뒤에서
  처음 만난 금액만 물었다.
- **수정** `common.pay_from_shape()` — 연락처·이메일처럼 **모양으로** 찾는다. 급여 낱말이
  금액을 직접 데리고 다니는 꼴만 받고, 여럿이면 모두 싣는다.
- **재발 방지** 없음. **표를 행·열 구조로 읽는 일반 해법은 아직 없다** — 남은 숙제다.
- **곁가지** 양산남부고·구월여중은 비어 있던 페이가 이 경로로 채워졌다.
- **발견 경위** 사용자 카드 스크린샷 + 원문 대조("다 나와있는데 왜 파악하지 못한걸까").
- **커밋** 0bf2067

### 2026-08-25  운영직·참가단위 공고가 게시됨 (범위 밖)
- **증상** `(재)포항문화재단 꿈의 스튜디오 운영인력(코디네이터) 채용`,
  `윤이상국제음악콩쿠르 스쿨콘서트 참가 학교 모집` 이 카드로 나갔다.
- **원인** `STAFF_EXCLUDE` 에 운영·코디 직군이 없었고, 참가자 규칙은 사람만 알았지
  '학교·기관을 받는 공모'를 몰랐다.
- **수정** `STAFF_EXCLUDE` 에 코디네이터·운영인력류 추가.
  `_PARTICIPANT_UNIT` 을 **별도 규칙으로** 뒀다 — `_HIRE_ROLE` 보호를 주지 않는다.
  '예술강사 지원사업 참여 학교 모집'처럼 직무 어휘가 섞여도 뽑는 대상은 학교지 사람이 아니다.
- **재발 방지** 회귀 케이스 9개로 검증(정상 공고가 안 걸리는지 포함).
- **발견 경위** 사용자 카드 스크린샷.
- **커밋** 0bf2067

### 2026-08-23  값이 태그 사이가 아니라 속성에 들어 마감을 못 읽음
- **증상** 남해정보산업고 공고가 마감 미확인으로 3회차까지 남아 사용자에게 알림이 갔다.
  화면에는 `접수기간 2026.08.21 - 2026.08.25` 가 뻔히 보였다.
- **원인** 경남교육청은 읽기 전용 정보를 입력폼으로 그린다 —
  `<input value="2026.08.21" disabled>`. `get_text()` 는 속성을 보지 않아 우리 본문에는
  `접수기간 시작일 - 종료일` 이라는 라벨만 남았다.
- **수정** `common._inline_input_values()` 가 입력칸 값을 **그 자리의 텍스트로** 바꿔 넣는다.
  처음엔 문서 끝에 몰아 붙였다가 되돌렸다 — 마감 추출은 라벨 근처의 날짜를 찾으므로
  값이 끝에 떨어져 있으면 여전히 못 읽는다.
- **재발 방지** 없음. 같은 꼴(속성에 값)이 다른 사이트에도 있으면 이제 자동으로 읽힌다.
- **발견 경위** 사용자가 마감 미확인 알림을 보고 원문과 대조해 제보.
- **커밋** 3fa0739

### 2026-08-23  원문 보관층의 '불변'이 본문 추출기 개선을 막고 있었다
- **증상** 위 입력칸 버그를 고쳤는데도 남해 마감이 계속 비었다.
- **원인** `rawstore` 가 page 를 `old or new` 로 저장해 **한 번 담기면 영영 안 바뀐다.**
  필드 추출기는 저장된 본문 위에서 다시 도니 소급됐지만, **본문을 뽑는 단계 자체가
  좋아진 경우엔 저장본이 옛 것이라 개선이 닿지 않는다.** '추출기를 고치면 과거분까지
  자기치유된다'는 이 프로젝트의 전제에 뚫려 있던 구멍이다.
- **수정** `_richer()` 로 **더 많이 읽어 낸 쪽**을 택한다(짧아지는 쪽은 안 받는다 —
  사이트 장애·로그인 게이트로 본문이 쪼그라들 수 있다). **`flush()` 와 `load()` 양쪽에**
  적용 — flush 만 고치면 파일은 좋아지는데 그 회차 재추출은 옛 본문을 봐 한 박자 늦는다.
- **재발 방지** 없음. 구조 자체를 바꿨다.
- **발견 경위** 입력칸 버그 수정 후 검증하다 "고쳤는데 왜 그대로지?"에서 파고들어 발견.
- **커밋** 3fa0739

### 2026-08-23  집계 포털이 준 '원문 링크'가 게시판 목록이었다
- **증상** 광신대 강사 초빙의 `공식 공고 페이지` 버튼이 공지사항 **목록**으로 갔다.
  게다가 그 목록을 원문으로 저장해 남의 글 첨부(`연구윤리제도화`)까지 붙어 있었다.
- **원인** `_origin_check` 가 officialUrl 을 검증 없이 원문으로 삼았다.
- **수정** `_looks_like_list()` 로 가려내고 `_find_detail_in_list()` 로 상세를 찾아 들어간다.
  못 찾으면 원문으로 저장하지 않고 `originIsList` 로 표시해, 마감 미확인 알림에
  '링크가 목록이라 바로 안 열린다'고 적어 헛걸음을 막는다.
- **한계** 광신대는 상세 링크가 `javascript:fView('42734')` 라 **주소가 없어 자동 교정이
  원리적으로 불가능하다.** href 가 진짜 주소인 사이트에서만 교정된다.
- **발견 경위** 사용자 제보("광신대는 링크도 잘못된 듯? 게시판으로 간다").
- **커밋** 3fa0739

### 2026-08-21  마감된 공고가 '기한 미정'으로 영영 살아남음
- **증상** 천안시립교향악단 예술감독 공고(8/14 마감)가 8/21 까지 접수중으로 걸려 있었다.
  원문에도 제목에도 `(~8/14)` 가 적혀 있었다.
- **원인** 원문 재추출에서 뽑은 마감을 **'오늘 이후'일 때만** 채택하는 가드가 거꾸로 작동했다.
  진짜 마감된 공고는 마감일이 영영 안 붙고, 그러니 그 아래 '지난 마감이면 내린다' 로직까지
  가지도 못했다. **방어 로직 둘이 서로를 무력화한 꼴.**
- **수정** 지난 마감도 채택하되 출처로 가른다 — 게시일보다 뒤여야 하고(게시일 자체를
  마감으로 오독한 경우 배제), 120일 넘게 지난 날짜는 다른 공고의 것으로 본다.
- **재발 방지** 소급 채택 시 `마감 소급: {공고} → {날짜}` 를 로그에 남긴다. 자주 뜨면
  마감 추출이 늦게 붙는다는 신호.
- **발견 경위** 사용자 제보("공고기간이 8월14일까지라고 적혀 있다").
- **커밋** 4572285

### 2026-08-21  서로 다른 공고 둘이 한 장으로 합쳐져 하나가 사라짐
- **증상** 영신여고의 `음악거점학교 시간강사`(시급 5만원)와 `음악중점학급 시간강사`
  (시급 2.5만원)가 한 건으로 병합돼 하나가 통째로 없어졌다.
- **원인** dedup 의 '자리 판별'이 악기·학년·분야 어휘만 보는데 두 공고 다 제목에 악기가
  없어 표식이 **양쪽 다 빈 집합**이 됐고, '판별 불가 → 보수적 병합' 규칙에 걸렸다.
  그 기본값 자체는 옳다(집계 포털판은 악기 태그가 없어 갈라 놓으면 같은 자리가 두 장으로
  뜬다) — 표식이 좁았던 게 문제.
- **수정** 학교 프로그램 이름(거점학교·중점학급·영재학급·방과후·자율동아리…)을 자리
  표식(`_FIELD_TOK`)에 추가.
- **재발 방지** 없음. 같은 자리끼리는 여전히 합쳐지는지 회귀 케이스로 확인.
- **발견 경위** 배포 전 전체 대조 중 `[내려감]` 목록에서 우연히 발견. **사용자는 몰랐다.**
- **커밋** 8f0f775

### 2026-08-21  담당업무가 다음 항목을 삼킴 / 예배시간이 근무시간 칸에 없음
- **증상** 성은교회 담당업무가
  `주일예배 및 성가대 피아노 반주자 · 예배시간 : 매주 일요일 11시~12시(1시간)`.
  뒤 조각은 근무시간이고 제 칸이 따로 있다.
- **원인** ① 근무시간 라벨 사전에 `예배시간` 이 없었다(교회 공고에서 예배시간 = 근무시간).
  ② `_find_duty` 가 항목 구분자로 나눈 조각을 둘까지 이어 붙이며 다음 항목을 삼켰다.
- **수정** 라벨 추가 + 조각이 `○○ :` 꼴로 시작하면 버린다. 다만 hwp 표는 라벨을 칸 맞춤으로
  `사 례 비 :` 처럼 띄어 쓰므로 **먼저 눕히고(squash_spaced_labels) 판정**해야 한다.
- **재발 방지** 없음.
- **발견 경위** 사용자 카드 스크린샷.
- **커밋** 8f0f775

### 2026-08-21  본문 발췌의 절반이 채용포털 화면 부속물
- **증상** 발췌 23건 중 8건이 `채용담당자 이름 김** 연락처 02`. 만족도 설문 위젯
  (`이 페이지의 내용과 편의성에 만족하십니까`)까지 실려 있었다. 기독정보넷은 본문 아래에
  **다른 교회 공고 목록**을 달고 와서, 성은교회 공고 요약이
  `탑동감리교회 드럼반주자 모집합니다` 였다 — 남의 공고가 우리 데이터에 들어와 있었다.
- **원인** `_EXCERPT_SKIP` 에 포털 화면 부속물 어휘가 없었다. 남의 공고는 줄마다 기관
  이름이 하나씩이라 '한 줄에 여럿' 규칙으로 못 걸렀다.
- **수정** 화면 부속물 어휘 + 마스킹 표식(`김**`) 배제. 남의 공고는 **그 줄의 기관명을
  이 공고의 org 와 견줘** 다르면 버린다.
- **곁가지(되돌린 시도)** '페이지에서 못 건지면 첨부까지 본다'는 폴백을 넣었다가 철회했다 —
  채움률은 오르지만 게시판 이용 안내문이 요약으로 올라왔다. 화면에 안 쓰이는 필드이므로
  **채움률보다 순도**를 택했다.
- **덤으로 잡은 것** 표를 편 줄 판정에 조사 `과`·`도` 를 쓰고 있었는데, `교과·학과·음악과`,
  `경기도·강원도` 의 끝 글자이기도 해서 표 나열 줄이 통째로 문장으로 통과하고 있었다.
- **발견 경위** 사용자 지시("정리해야지") 후 전체 필드 훑기.
- **커밋** f97c495

### 2026-08-21  교육청 게시판 메타표를 통째로 못 읽어 기관명이 뭉침
- **증상** 학교 공고가 전부 `강원교육청(학교 채용)`·`인천교육청(학교 채용)` 으로 뭉쳤고,
  주소·페이·모집·담당업무·자격이 전부 빈칸이었다(학성초 밴드부 강사).
- **원인** 시도교육청 구인 게시판은 상세 위쪽에 고정 메타표(기관명·주소·전화·담당자·
  마감일자)를 얹는데 **라벨과 값 사이에 구분자가 없다**(콜론 없음). `라벨 : 값` 규칙이
  통째로 헛돌았고, 주소는 다음 칸까지 딸려와 길이 초과로 버려졌다.
- **수정** `common.parse_meta_table()` 이 라벨 집합을 알고 통째로 읽는다. 기관명은
  이름꼴(`_ORG_SHAPE`)로, 주소는 시도명 시작으로 검증해 받는다 — 반쯤 맞는 주소를 들이면
  공고가 엉뚱한 시도로 넘어가므로 못 믿을 값은 버리고 기존 경로에 맡긴다.
- **재발 방지** **칸 이름은 교육청마다 다르다**(모집직종·학교/기관·마감일…). 새 교육청을
  붙이면 `_META_LAB` 에 그 표의 칸 이름을 추가할 것.
- **발견 경위** 사용자 카드 스크린샷 + 원문 대조 지시.
- **커밋** 569c15a

### 2026-08-21  hwp 뷰어 렌더 본문이 낱말마다 벌어져 금액·인원 추출 실패
- **증상** `40,000 원`, `1 명`, `화요일 , 목요일`, `지도 ( 보컬 ) 및`. 금액·인원 정규식이
  붙은 표기를 기대해 통째로 빗나갔다. 시간당 4만원짜리 공고의 페이가 빈칸이었다.
- **원인** hwp 뷰어가 글자 단위로 렌더한 것을 그대로 긁었다.
- **수정** `common.tidy_spacing()` — 사람이 읽는 꼴로 되돌린다. **추출 경로가 여럿이라
  전용 추출기(`_find_duty`·`_find_qualification`)에도 같이 태워야 한다.** `_clean_field`
  만 고쳤더니 담당업무·자격이 벌어진 채로 화면에 나왔다.
- **주의** 가운뎃점(·)과 마침표는 건드리지 않는다 — 항목을 ` · ` 로 이어 붙인 뒤 다시
  태우면 구분자가 `우대· 해당` 처럼 망가지고, 마침표는 날짜(`2026. 9.`)를 붙여 버린다.
  단위(`원·명·인`)만 숫자에 붙인다 — 아무 한글에나 붙이면 `11:50 토요일` 이
  `11:50토요일` 이 된다.
- **발견 경위** 위 메타표 건 조사 중 함께 발견.
- **커밋** 569c15a, 0bf2067

### 2026-08-21  항목 번호 두 층을 섞어 다뤄 날짜를 먹거나 첫 항목만 남김
- **증상** `9.21.(월)~9.23.(수)` 가 `21.(월)~…` 로 잘렸다. 반대로 담당업무·자격은
  `1) …` 첫 항목만 남고 `2) …` 이후가 사라졌다.
- **원인** 원문의 번호 매김은 두 층이다 — `4. 지원 자격 1) … 2) …` 에서 `N.` 은 문서의
  절 경계, `N)` 은 그 절 안의 목록 항목. 이 둘을 같이 다루면 둘 다 망가진다.
- **수정** 절 경계에서는 끊고, 목록 번호만 구분자로 쓴다. 접두 제거도 괄호꼴만.
- **재발 방지** 없음.
- **발견 경위** 값 정리 후 전체 대조 중 회귀로 발견(내가 만든 회귀를 배포 전에 잡았다).
- **커밋** f97c495

### 2026-08-21  모집 인원 칸에 표 전체가 들어옴
- **증상** 제물포 구립합창단 모집 칸이
  `성악지도자 1명 제물포구 구립 여성합창단 반주자 1명 … 단무장 1명 ▢`.
  카드 위에 이미 태그로 있는 악기(`피아노`)도 앞에 덧붙고 있었다.
- **원인** 모집 표를 통째로 평탄화한 값을 그대로 실었다.
- **수정** `common.tidy_personnel()` — 못 읽을 만큼 길 때만 합계로 접는다(`총 5명`).
  짧으면 파트별 인원이 그대로 쓸모 있다(`보컬 강사 1명, 기악 1명`). 모집 행의 악기
  접두 제거(jobs.js · staticgen.py).
- **재발 방지** 없음.
- **발견 경위** 사용자 카드 스크린샷("피아노는 왜 있는거임????").
- **커밋** bda7544, f97c495

---

## 이 목록에서 읽히는 것 (2026-08-25 시점)

- **버그의 대부분은 '추출기가 없어서'가 아니라 '있는데 안 도는' 것이었다.** enrich 18일,
  본문 보관층의 개선 차단, 마감 가드의 역작동 — 셋 다 코드는 있는데 닿지 않았다.
- **가장 잘 잡아낸 검수 수단은 사용자의 카드 스크린샷이다.** 자동 헬스체크는 '값이
  그 항목답게 생겼는가'까지만 보므로, 화면에 이상한 값이 나가도 조용하다.
- **두 번째는 배포 전 전체 대조다.** 영신여고 병합처럼 사용자도 모르던 손실을 여기서 잡았다.
- **`except` 로 예외를 삼키면 매일 80건이 실패해도 아무도 모른다.**
